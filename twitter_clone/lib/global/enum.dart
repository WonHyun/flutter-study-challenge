enum LoginType {
  google,
  apple,
}

enum PolicyType {
  personalize,
}

enum MainScreenType {
  home,
  search,
  posting,
  activity,
  user,
}

enum PostVisiblity {
  public,
  private,
}

enum ActivityType {
  all,
  replies,
  mentions,
  verified,
  follow,
  like,
  repost,
  quote,
}

enum SettingMenu {
  friends,
  notifications,
  privacy,
  account,
  help,
  about,
}

enum PrivacyMenu {
  private,
  mentions,
  muted,
  hiddenWords,
  followerProfiles,
}

enum ProfileTab {
  threads,
  replies,
}

enum BoxName {
  settings,
}

enum UserSetting {
  themeMode,
  privateProfile,
}
