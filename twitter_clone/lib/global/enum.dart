enum LoginType {
  google,
  apple,
}

enum PolicyType {
  personalize,
}

enum MainScreenType {
  home,
  search,
  posting,
  activity,
  user,
}

enum PostVisiblity {
  public,
  private,
}

enum ActivityType {
  all,
  replies,
  mentions,
  verified,
  follow,
  like,
  repost,
  quote,
}
